alt + Return
	alacritty

alt + p
	rofi -show run -show-icons -display-run "başlat:"	

super + w
	rofi-pass --last-used

super + p
	arandr

alt + w
	chromium

alt + Escape
	bund -r

ctrl + alt + shift + t
	bund -o 

alt + ctrl + shift + Escape
	bund -q

alt + v
	clipmenu

alt + shift + Return
	workspaces

alt + shift + Escape
  themes

super + l
  xset dpms force suspend && slock
